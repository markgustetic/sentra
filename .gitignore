bin/
dist/
*.test
*.out
.vscode/
.idea/
*.swp
.DS_Store

# Local config / secrets
.env
.env.*
!.env.example
sentra.yaml
*.setup-draft
.sentraignore

# `sentra local` dev config (never the real sentra.yaml)
.sentra-local.yaml
.sentra-local/

# AWS smoke-test payload (`just aws-seed` / `just aws-smoke`)
aws-test-data/

# Visual brainstorming mockups (superpowers)
.superpowers/
