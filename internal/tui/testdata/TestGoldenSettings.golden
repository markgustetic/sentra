Settings

  bucket   golden-bucket
  prefix   (unset)
  region   us-east-1
  keyring  off
  config   /repo/sentra.yaml

▍ Scheduled backups
    jobs: cadence, next run, edit, and drill-in
  Recovery kit
    render the printable recovery document
  Change passphrase
    rotate the repository passphrase
  Re-run setup
    reconfigure the backend and repository
  Welcome splash   [on]
    show the logo screen at launch (applies next launch)
  Forget keyring passphrase
    remove the OS keyring entry and disable keyring lookup

↑↓ move   ⏎ open / toggle